---
name: Mindustry Server
version: 1.0.0
slug: mindustry_server
description: Mindustry game server with Home Assistant integration
url: https://github.com/Moozzuzz/mindustry-server-addon
init: false
arch:
  - aarch64
  - amd64
options:
  port: 6567
  server_name: "Mindustry Server"
  log_level: info
  max_players: 10
  pvp: false
  strict: false
  auto_restart: true
  restart_hour: 3
schema:
  port: port(1025,65535)
  server_name: str(1,50)
  log_level: list(trace|debug|info|notice|warning|error|fatal)
  max_players: int(1,256)
  pvp: bool
  strict: bool
  auto_restart: bool
  restart_hour: int(0,23)
